commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-secondary-20260911
  dequeue:
    conditions:
      - sender = livastic-vm3-primary-20260911
queue_rules:
  - name: vm3-source-head-freshness
    queue_conditions:
      - base = vm3-headfresh-target-20260918
    merge_conditions:
      - check-success = vm3-headfresh-gate
    merge_method: merge
    batch_size: 2
merge_queue:
  mode: serial
  max_parallel_checks: 1
